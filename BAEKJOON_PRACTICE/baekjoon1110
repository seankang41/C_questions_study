*더하기 사이클 

***문제
>이 문제 특정한 규칙에 따라 몇번 진행해야 다시 원래의 수로 돌아오는지 출력하는 문제

#include <stdio.h>
#include <stdlib.h>


int main(void) {
  int n = 0, num; scanf("%d", &num);
  int new_num = num;

  do{
    new_num = (new_num % 10) * 10 + ((new_num / 10) + (new_num % 10)) % 10;
    ++n;
  }while (new_num != num);

  printf("%d", n);
  return 0;
}
