#include <stdio.h>

int main(void) {
  int a, result=0;
  int remainder[10];
  for(int i=0; i<10; i++) {
    scanf("%d", &a);
    remainder[i] = (a % 42);
  }
    
  for(int i=0; i<10; i++) {
    int cnt=0; 
    for(int j=i+1; j<10; j++) { 
        if(remainder[i] == remainder[j]) cnt++;
    }
    if (cnt == 0) result++; 
  }
    
  printf("%d", result);

  return 0;
}
